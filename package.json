{
  "private": true,
  "version": "0.0.1",
  "name": "oceanfront-repo",
  "packageManager": "yarn@1.22.17",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build-dev": "turbo run build-dev",
    "package": "turbo run package",
    "clean": "turbo run clean && rm -rf node_modules",
    "coverage": "turbo run coverage",
    "demo": "turbo run demo",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.9.14"
  },
  "resolutions": {
    "vue": "~3.5.25",
    "minimatch": "^10.2.5",
    "glob": "^13.0.6"
  }
}
